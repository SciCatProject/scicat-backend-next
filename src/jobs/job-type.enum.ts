export enum JobType {
  Archive = "archive",
  Retrieve = "retrieve",
  Public = "public",
}

export enum DatasetState {
  retrieve = "retrievable",
  archive = "archivable",
  public = "isPublished",
}
