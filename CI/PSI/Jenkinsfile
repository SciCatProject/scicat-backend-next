node {
       stage('Checkout'){

          checkout scm
       }


       stage('Build Docker'){

            sh 'docker build -f CI/PSI/Dockerfile.test . -t catamel_test'
       }
       stage('Unit Test Catanie '){
            sh 'docker run  --privileged -t catamel_test npm run -- ng test --watch false --single-run true'
       }
       stage('e2e Test Catanie '){
            sh 'docker run  --privileged -t catamel_test npm run e2e --environment qa'
       }

}
