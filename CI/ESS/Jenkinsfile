node ('docker') {
       stage('Checkout'){

          checkout scm
       }


       stage('Build Docker') {
            sh 'docker-compose  -f CI/ESS/docker-compose.yaml build'
       }
       stage('SuperTest Catamel ') {
            sh 'docker run  --privileged -t catamel_test npm run test'
       }

}
